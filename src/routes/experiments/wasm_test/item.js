import { base } from "$app/paths";
import thumb from './thumb.jpg'

let name = 'wasm_test'

export default {
    image: thumb,
    title: "wasm_test",
    subtitle: "smoell test",
    tags: ["rust", "wasm"],
    link: `${base}/experiments/${name}`,
};
